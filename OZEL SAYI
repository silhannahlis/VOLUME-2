#include <stdio.h>

int main() {
    int n,u,first,second;
    int i;
    
    printf(" 4 basamaklı sayı:");scanf("%d",&n);
    first = n /100;
    second = n %100;
    u= (first+second)*(first+second);
    (n==u)? printf("ÖZEL SAYI\n") : printf("özel sayı DEĞİL");
    //(n==(first+second)*(first+second))? printf("%d ÖZEL SAYI",n) : printf("özel sayı DEĞİL");
    
    //buda özel sayı bulma for döngüsü
    /*
     for(i=1000; i<10000;i++){
        first = i /100; 
        second = i %100; ;
        u= (first+second)*(first+second);
        (i==u)? printf("%d ÖZEL SAYI;\n",i) : printf("");
        }
    */
    }
